#pragma once

/*
	The raw data for one aspect of the object, and how it interacts with the world. 
	"Labels the Entity as possessing this particular aspect". Implementations typically use structs, classes, or associative arrays.

	See: https://en.wikipedia.org/wiki/Entity_component_system

	A Component stores only data and are logicless objects. Logic is handled by Systems
*/

class IComponent
{
public:

private:
	
};